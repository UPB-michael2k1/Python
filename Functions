### This is a simple function to test the return value - none ( a keyword ) 

def strange_function(n):
    if(n % 2 == 0):
        return True
print(strange_function(2))
print(strange_funcion(5))

### OUTPUT 

# The function will return True ( 1st print - true for even number )
# will return none as the number is not even
